import { Section, SectionTheme } from '@/ui/Section/Section';
import React from 'react';

export function Books() {
  return <Section className="books" id="books" theme={SectionTheme.GRAY} center>Books</Section>;
}
