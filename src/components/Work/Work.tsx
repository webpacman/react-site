import { Section, SectionTheme } from '@/ui/Section/Section';
import React from 'react';

export function Work() {
  return <Section className="work" id="work" theme={SectionTheme.GRAY}>Work</Section>;
}
