import { Section, SectionTheme } from '@/ui/Section/Section';
import React from 'react';

export function Achievements() {
  return (
    <Section className="achievements" id="achievements" theme={SectionTheme.GRAY} center>
      achievements
    </Section>
  );
}
