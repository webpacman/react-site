import { Section, SectionTheme } from '@/ui/Section/Section';
import React from 'react';

export function Footer() {
  return <Section className="footer" theme={SectionTheme.GRAY} Tag="footer">Footer</Section>;
}
